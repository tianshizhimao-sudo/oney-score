{
  "name": "oney-score-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for the Bank-Ready Score report flow — receives POST /score-report/submit, sends three emails via Resend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.100.0"
  }
}
